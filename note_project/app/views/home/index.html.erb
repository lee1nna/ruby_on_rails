<h1>전체 글 목록입니다!</h1>
<a href="/home/new"> 새 글 쓰기 </a>
<br>
<br>
<% Post.all.each do |p| %>
  제목 : <%=p.title%><br/>
  내용 : <%=p.content%><br /><br>

  <%# a태그를 이용하는 방법 %>
  <a href="/home/destroy/<%=p.id%>"> [삭제1] </a>
  <%# link_to를 이용하지만, url을 전부 입력하는 방법 %>
  <%=link_to("[삭제2]", "/home/destroy/#{p.id}")%>
  <%# link_to와 as(별칭)을 이용하는 방법 %>
  <%=link_to("[삭제3]", post_destroy_path(p.id))%>
  <%# link_to와 do-end를 이용하는 방법 %>
  <%=link_to(post_destroy_path(post_id: p.id)) do%>
    <button>삭제4</button>
  <%end%>

  <a href="/home/edit/<%=p.id%>">[수정]</a><br>
  -------------------------------------<br />
<% end %>


