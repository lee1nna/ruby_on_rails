<h1>전체 글 목록입니다!</h1>
<a href="/home/new"> 새 글 쓰기 </a>
<br>
<br>
<% Post.all.each do |p| %>
  제목 : <%=p.title%><br/>
  내용 : <%=p.content%><br /><br>

<% if false %>
  <%# a태그를 이용하는 방법 %>
  <a href="/home/destroy/<%=p.id%>"> [삭제1] </a>
  <%# link_to를 이용하지만, url을 전부 입력하는 방법 %>
  <%=link_to("[삭제2]", "/home/destroy/#{p.id}")%>
  <%# link_to와 as(별칭)을 이용하는 방법 %>
  <%=link_to("[삭제3]", post_path(post_id: p.id))%>
<% end %>

  <%# link_to와 do-end를 이용하는 방법 %>
  <%=link_to(post_path(p.id), method: :delete) do%>
    <button>삭제</button>
  <%end%>

  <%=link_to(edit_post_path(p.id)) do%>
    <button>수정</button>
  <%end%><br>
  -------------------------------------<br />
<%end%>
